<!DOCTYPE html>
<html>
<head>
  <title>Home | Discord Login</title>
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap');
    
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    
    body {
      font-family: 'Inter', sans-serif;
      background: #000000;
      color: #ffffff;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      background-image: 
        radial-gradient(circle at 20% 50%, rgba(255, 156, 0, 0.1) 0%, transparent 50%),
        radial-gradient(circle at 80% 20%, rgba(255, 216, 74, 0.1) 0%, transparent 50%),
        radial-gradient(circle at 40% 80%, rgba(255, 119, 0, 0.1) 0%, transparent 50%);
      animation: backgroundShift 15s ease-in-out infinite;
    }
    
    @keyframes backgroundShift {
      0%, 100% { filter: hue-rotate(0deg); }
      50% { filter: hue-rotate(10deg); }
    }
    
    .container {
      background: #1a1a1a;
      padding: 3rem 2.5rem;
      border-radius: 20px;
      box-shadow: 
        0 20px 40px rgba(0, 0, 0, 0.5),
        0 0 0 1px rgba(255, 156, 0, 0.1),
        inset 0 1px 0 rgba(255, 255, 255, 0.1);
      text-align: center;
      min-width: 400px;
      position: relative;
      overflow: hidden;
    }
    
    .container::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: linear-gradient(45deg, transparent 30%, rgba(255, 156, 0, 0.03) 50%, transparent 70%);
      pointer-events: none;
    }
    
    h1 {
      font-size: 2.5rem;
      font-weight: 700;
      margin-bottom: 2rem;
      background: linear-gradient(45deg, #FFD84A, #FF9C00);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      text-shadow: 0 0 30px rgba(255, 156, 0, 0.3);
      position: relative;
      animation: glow 3s ease-in-out infinite alternate;
    }
    
    @keyframes glow {
      0% { filter: brightness(1); }
      100% { filter: brightness(1.2); }
    }
    
    p {
      font-size: 1.1rem;
      margin-bottom: 1.5rem;
      color: #cccccc;
      line-height: 1.6;
    }
    
    p strong {
      color: #FF9C00;
      font-weight: 600;
      text-shadow: 0 0 10px rgba(255, 156, 0, 0.3);
    }
    
    a {
      color: #FFD84A;
      text-decoration: none;
      font-weight: 500;
      transition: all 0.3s ease;
      position: relative;
    }
    
    a:hover {
      color: #FF9C00;
      text-shadow: 0 0 8px rgba(255, 156, 0, 0.4);
    }
    
    a:not(.btn) {
      border-bottom: 1px solid transparent;
    }
    
    a:not(.btn):hover {
      border-bottom-color: #FF9C00;
    }
    
    .btn {
      display: inline-block;
      background: linear-gradient(45deg, #FFD84A, #FF9C00);
      color: #000000;
      padding: 1rem 2rem;
      border-radius: 12px;
      font-weight: 600;
      font-size: 1.1rem;
      margin-top: 1rem;
      box-shadow: 
        0 10px 20px rgba(255, 156, 0, 0.2),
        inset 0 1px 0 rgba(255, 255, 255, 0.2);
      transition: all 0.3s ease;
      border: none;
      cursor: pointer;
      position: relative;
      overflow: hidden;
    }
    
    .btn::before {
      content: '';
      position: absolute;
      top: 0;
      left: -100%;
      width: 100%;
      height: 100%;
      background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
      transition: left 0.5s ease;
    }
    
    .btn:hover::before {
      left: 100%;
    }
    
    .btn:hover {
      transform: translateY(-2px);
      box-shadow: 
        0 15px 30px rgba(255, 156, 0, 0.3),
        inset 0 1px 0 rgba(255, 255, 255, 0.3);
      background: linear-gradient(45deg, #FFD84A, #FF7700);
    }
    
    .btn:active {
      transform: translateY(0);
      box-shadow: 
        0 5px 15px rgba(255, 156, 0, 0.2),
        inset 0 1px 0 rgba(255, 255, 255, 0.2);
    }
    
    /* Discord icon effect */
    .btn::after {
      content: '🎮';
      margin-left: 0.5rem;
      display: inline-block;
      transition: transform 0.3s ease;
    }
    
    .btn:hover::after {
      transform: rotate(15deg) scale(1.1);
    }
    
    /* Responsive design */
    @media (max-width: 480px) {
      .container {
        margin: 1rem;
        padding: 2rem 1.5rem;
        min-width: auto;
      }
      
      h1 {
        font-size: 2rem;
      }
      
      .btn {
        padding: 0.875rem 1.5rem;
        font-size: 1rem;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Welcome to My Website</h1>
    <% if (user) { %>
      <p>You are logged in as <strong><%= user.username %></strong></p>
      <a href="/profile">Go to Profile</a> | 
      <a href="/auth/logout">Logout</a> | 
      <a href="https://virelia.live/">Go to Main Website</a> | 
      <a href="https://virelia.live/contact">Visit Contact to Get Support</a>
    <% } else { %>
      <a href="/auth/discord" class="btn">Login with Discord</a>
      <br><br>
      <a href="https://virelia.live/">Go to Main Website</a> | 
      <a href="https://virelia.live/contact">Get Support</a>
    <% } %>
  </div>
</body>
</html>